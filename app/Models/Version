<?php

namespace App\Models;

use Illuminate\Database\Eloquent\Model;

class Version extends Model
{
    // Optionally, define the table if it's not following Laravel's conventions
    // protected $table = 'versions';

    // If needed, define the fillable or guarded properties
    // protected $fillable = ['version', 'link'];
}
